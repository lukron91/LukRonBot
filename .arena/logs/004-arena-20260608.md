# Log 004 — arena-agent (2026-06-08)

**Model:** Arena.ai Agent  
**Gałąź:** `dev`

---

## Commit 1: `730c3e5` — Fix: guildId w bot-settings, zduplikowane onClick w users, window.confirm -> Modal

### Co zmieniono
- `panel/app/dashboard/bot-settings/page.jsx`:
  - Dodano `import { useSearchParams } from "next/navigation"`
  - Dodano `const searchParams = useSearchParams()` i `const guildId = searchParams.get("guild")` — `guildId` było używane w `refreshAllCommands()` i `manageCommands()` ale nigdzie niezadeklarowane (błąd: `guildId is not defined`)
  - Dodano `const [cmdRegType, setCmdRegType] = useState("guild")` — używane w JSX przy przyciskach rejestracji ale niezadeklarowane
  - Dodano `const [cmdMessage, setCmdMessage] = useState("")` — używane w JSX ale niezadeklarowane
  - `manageCommands()`: zmieniono `type: cmdSubTab === 'global' ? 'global' : 'guild'` na `type: cmdRegType` — `cmdSubTab` miał wartość `'registration'` co dawało błędny typ
  - `manageCommands()`: dodano `setCmdMessage(...)` przy sukcesie i błędzie

- `panel/app/dashboard/moderation/users/page.jsx`:
  - Przepisany w całości — błąd parsowania JSX linia 328: każdy `<button>` zakładki miał zduplikowany `onClick` jako tekst wewnątrz elementu
  - Zamieniono `window.confirm()` na `<Modal>` we wszystkich 3 miejscach: `deleteWarn()`, `unmuteUser()`, `unbanUser()`
  - Dodano stan `confirmModal` do obsługi potwierdzeń
  - Naprawiono hardkodowane kolory → CSS variables
  - Naprawiono `style="..."` jako string (invalid JSX) → `style={{ ... }}` jako obiekt

---

## Commit 2: `68e818d` — Feat: system tokenów przycisków + styl filled/outline w ustawieniach motywu

### Co zmieniono
- `panel/app/globals.css`:
  - Dodano tokeny CSS w `:root`: `--btn-height: 2.4rem`, `--btn-padding`, `--btn-gap`, `--btn-font-size`, `--btn-font-weight`, `--btn-border-width`, `--btn-color-*`
  - `.btn-base`: zmieniono z `padding` wertykalnego na `height: var(--btn-height)` — stała wysokość niezależna od zawartości
  - Dodano `white-space: nowrap` i `flex-shrink: 0`
  - Dodano wariant outline przez selektor `html.btn-outline-mode .btn-*` — hover wypełnia, bez hovera tylko ramka
  - Usunięto `width: fit-content; margin: 0 auto` — przyciski nie powinny być wyśrodkowane globalnie

- `panel/lib/theme-context.jsx`:
  - Dodano `buttonStyle: 'filled'` do `DEFAULT_THEME`
  - W `useEffect` przy każdej zmianie motywu: dodaje/usuwa klasę `btn-outline-mode` z `document.documentElement`

- `panel/app/dashboard/theme/page.jsx`:
  - Dodano `const buttonStyle = theme?.buttonStyle || 'filled'`
  - Nowa sekcja "Styl przycisków" z wizualnym podglądem obu opcji (filled/outline)
  - Sekcja "Podgląd" pokazuje teraz 4 typy przycisków w aktualnym stylu zamiast jednego

---

## Commit 3: `5093f4e` — Fix: odstępy między przyciskami w modułach, globalne klasy btn-row/btn-col/btn-row-end

### Co zmieniono
- `panel/app/globals.css`:
  - Dodano globalne klasy kontenerów: `.btn-row`, `.btn-row-lg`, `.btn-col`, `.btn-row-end`
  - Każda z gap `0.75rem` lub `1rem`, flexbox, wrap — zamiast inline `style={{ display: 'flex', gap: ... }}`

- `panel/app/dashboard/bot-settings/page.jsx`:
  - `.module-actions` → `<div className="btn-row">` — naprawiony brak odstępu między przyciskami "Test modułu" i "Przeładuj"
  - `command-setup` i `command-bulk-actions` z inline style → `<div className="btn-row">`
  - Przyciski toastu (kopiuj/zamknij): dodano `height: 'auto'` żeby nie były rozciągane przez `--btn-height`

---

## Commit 4: `5c68a9d` — Fix: baner — poprawne object-fit, overlay fade do tła, content przy dolnej krawędzi

### Co zmieniono
- `panel/app/dashboard/layout.jsx`:
  - `.top-bar-bg`: usunięto `min-width: 100%; min-height: 100%` → samo `width: 100%; height: 100%; object-fit: cover` + `display: block`
  - `.top-bar-overlay`: `top/left/right/bottom: 0` → `inset: 0`; gradient zmieniony na fade do `var(--bg-color)` (działa z dark i light mode)
  - `.top-bar-content`: zmieniono z `position: relative; height: 100%; padding-top: 50px` na `position: absolute; inset: 0; align-items: flex-end` — zawartość przy dolnej krawędzi banera
  - Overlay gradient przeniesiony do inline `style` żeby używał `var(--bg-color)` runtime

---

## Commit 5: `bd2e156` — Fix: baner — usunięty padding z .main-content w globals.css

### Co zmieniono
- `panel/app/globals.css`:
  - `.main-content`: `padding: 2rem` → `padding: 0` — globalna reguła nadpisywała lokalny `margin: 0; padding: 0` w layout.jsx i wciskała baner o 2rem z lewej i prawej
  - Dodano `.page-content { padding: 2rem }` jako opcjonalna klasa dla stron które jej potrzebują (żadna aktualnie nie korzysta — każda ma własny padding w root div)

---

## Commit 6: `862d188` — Feat: globalne klasy modal-* w globals.css, users/page przepięty na system motywu

### Co zmieniono
- `panel/app/globals.css`:
  - Nowa sekcja MODAL STANDARDS — gotowe klasy do użycia w każdym przyszłym oknie:
    - `.modal-tabs` / `.modal-tab` / `.modal-tab.active` — zakładki używające `var(--accent-color)`
    - `.modal-tab-content` — padding treści zakładki
    - `.modal-section` / `.modal-section-title` — sekcja z tytułem w accent-color
    - `.modal-input` / `.modal-select` / `.modal-textarea` — inputy z focus border accent
    - `.modal-info-row` + `.danger` / `.warning` / `.success` — wiersz z info (aktywna kara)
    - `.modal-message.success` / `.modal-message.error` — komunikaty z rgba tłem
    - `.modal-empty` — komunikat pustej listy

- `panel/components/Modal.jsx`:
  - `modal-close-custom`: dodano `height/width: var(--btn-height)` — X respektuje token rozmiaru

- `panel/app/dashboard/moderation/users/page.jsx`:
  - Zakładki: lokalny `tabs/tab` → globalne `modal-tabs/modal-tab`
  - Inputy: lokalne `action-input/select` → globalne `modal-input/select/textarea`
  - Wiersze kar: `active-punishment-item` → `modal-info-row warning/danger`
  - Przyciski: `action-button warn/mute/ban` → `btn-base btn-warning/standard/danger`
  - Komunikat: `action-message` → `modal-message`
  - Usunięto ~60 linii lokalnego CSS zastąpionego globalnym

---

## Commit 7: `f427e85` — Feat: zakładka Historia kar w oknie użytkownika

### Co zmieniono
- `panel/app/dashboard/moderation/users/page.jsx`:
  - Nowe stany: `history`, `historyLoading`
  - Nowa funkcja `fetchHistory(userId)` — używa istniejącego endpointu bota:
    `GET /api/guilds/:guildId/punishments/:userId` → `{ success, punishments: [...] }`
    Endpoint już istnieje w `bot/modules/moderation.js` linia 83, zwraca wszystkie kary posortowane od najnowszej
  - `openActionMenu`: dodano `fetchHistory` do `Promise.all` — historia ładuje się razem z otwarciem okna
  - `closeActionMenu`: `setHistory([])` — czyszczenie przy zamknięciu
  - `executeAction` po sukcesie: dodano `fetchHistory` do `Promise.all` — historia odświeża się po nowej karze
  - Nowa stała `PUNISHMENT_STYLE` — mapowanie typów (warn/mute/ban/kick/unmute/unban) na ikonę, label, klasę `modal-info-row`
    Przyszły agent dodaje nowe typy tylko tu, bez zmiany reszty kodu
  - Nowa zakładka "Historia" z ikoną FiList w `modal-tabs`
  - Zawartość zakładki: lista `modal-info-row` z typem, powodem, datą, moderatorem, czasem trwania
  - Przycisk Odśwież wewnątrz zakładki
  - `.history-list` w style jsx: max-height 400px, overflow-y auto, gap 0.5rem

### Kompatybilność z bazą
- Panel używa tylko istniejącego endpointu GET — nie wymaga zmian w bocie
- Schema bota: `{ guildId, userId, moderatorId, type, reason, duration, expiresAt, createdAt }`
- Panel wyświetla: `type`, `reason`, `createdAt`, `moderatorId`, `duration` — wszystkie pola już są w bazie
- Przyszły agent implementujący unmute/unban powinien zapisywać je jako osobne dokumenty z `type: 'unmute'/'unban'` — panel automatycznie je wyświetli przez `PUNISHMENT_STYLE`

---

## Commit 8: `56dbfca` — Refactor: sidebar - Przegląd i Konfiguracja do SERWER, Motyw do USTAWIENIA

### Co zmieniono
- `panel/app/dashboard/layout.jsx`:
  - Usunięta kategoria OGÓLNE i PANEL
  - Przegląd + Konfiguracja ogólna → kategoria SERWER
  - Motyw → kategoria USTAWIENIA (przedostatnia)
  - ZARZĄDZANIE BOTEM zostaje na samym dole
  - Kolejność kategorii: NAWIGACJA → SERWER → MODERACJA → TICKETY → AUTO-MOD → POWITANIA → LOGI → USTAWIENIA → ZARZĄDZANIE BOTEM

---

## Commit 9: `8b29739` — Feat: panel zarządzania rolami + moduł roles.js + WebSocket

### Co zmieniono
- `panel/app/dashboard/roles/page.jsx` — NOWY PLIK:
  - Lista ról z kolorem, nazwą, liczbą członków, tagami
  - Akcje per rola: przesuń góra/dół, członkowie, nadaj/odbierz, kopiuj, edytuj, usuń
  - Modal tworzenia/edycji: nazwa, color picker, hoist/mentionable, 19 uprawnień
  - Modal "Członkowie roli"
  - Modal "Nadaj/odbierz" z wyszukiwarką
  - Modal usuwania z potwierdzeniem
  - WebSocket klient — auto-odświeżanie po eventach Discord
  - Fallback polling co 10s gdy WS niedostępny
  - Badge Real-time/Polling w headerze
  - Error state + safeFetch

- `bot/modules/roles.js` — NOWY PLIK:
  - GET  /api/guilds/:id/roles/detailed — lista ról z pełnymi detalami
  - POST /api/guilds/:id/roles — utwórz rolę
  - PATCH /api/guilds/:id/roles/:roleId — edytuj rolę
  - DELETE /api/guilds/:id/roles/:roleId — usuń rolę
  - POST /api/guilds/:id/roles/:roleId/copy — kopiuj (auto-nazwa "kopia N")
  - POST /api/guilds/:id/roles/:roleId/move — przesuń pozycję
  - GET /api/guilds/:id/roles/:roleId/members — członkowie roli
  - POST /api/guilds/:id/roles/:roleId/members/:userId — nadaj/odbierz
  - Walidacja: brak edycji ról managed (bot/integracje)
  - Baza NIE jest potrzebna — wszystko przez Discord API

- `bot/index.js`:
  - Dodano `const http = require('http')` i `const { WebSocketServer } = require('ws')`
  - `app.listen()` zamienione na `http.createServer(app)` + `httpServer.listen()`
  - WebSocketServer na ścieżce `/ws`, klienci subskrybują po `?guildId=`
  - Discord eventy → broadcast: roleCreate, roleUpdate, roleDelete, guildMemberUpdate
  - UWAGA: wymaga `npm install ws` w katalogu bot/

- `panel/app/dashboard/layout.jsx`:
  - Dodano link "Zarządzanie rolami" w kategorii SERWER z ikoną FiShield

---

## Commit 10: `(pending)` — Fix: JSX błąd parsowania w roles/page.jsx

### Co zmieniono
- `panel/app/dashboard/roles/page.jsx`:
  - Linia 423: brakujący `)` zamykający arrow function w onChange dla toggle hoist
  - Linia 428: to samo dla toggle mentionable
  - `onChange={e => setRoleForm(p => ({ ...p, hoist: e.target.checked })}` → `onChange={e => setRoleForm(p => ({ ...p, hoist: e.target.checked }))}`

---

## Commit 11: `b2b1614` — Refactor: panel ról — przebudowa UI na dwukolumnowy layout

### Co zmieniono
- `panel/app/dashboard/roles/page.jsx` — przepisany w całości:

  **Layout:**
  - Dwukolumnowy: lewa (300px) = lista ról, prawa = edytor szczegółu
  - Całość zajmuje dostępną wysokość ekranu (calc 100vh - 200px)
  - Border wspólny dla obu kolumn, overflow hidden

  **Lewa kolumna (lista):**
  - Wyszukiwarka ról z ikoną i przyciskiem czyszczenia
  - Każda rola: kolorowa kropka, nazwa, liczba członków, tagi (wydzielona/bot)
  - Akcje per rola (przesuń góra/dół, kopiuj) widoczne po hover
  - Rola zaznaczona = aktywna z accent border-left
  - Role managed (bot) = wyszarzone z tagiem "bot"
  - Badge real-time/polling w nagłówku

  **Prawa kolumna (edytor):**
  - Stan pusty z grafiką gdy nic nie zaznaczone
  - Header: nazwa, kolor, przyciski Członkowie/Nadaj+odbierz/Usuń
  - Notice dla ról managed — nie można edytować
  - Sekcja Podstawowe: nazwa, color picker z hex + reset, toggles hoist/mentionable z opisami
  - Sekcja Uprawnienia: 37 uprawnień pogrupowanych w 4 grupy (Ogólne/Członkowie/Wiadomości/Kanały), każde z opisem, wyszukiwarka, przyciski Wszystkie/Żadne
  - Pasek zapisu: pojawia się tylko gdy są zmiany, komunikat "Masz niezapisane zmiany"
  - Wykrywanie zmian (hasChanges) — przycisk nieaktywny gdy brak zmian

  **Modalne:**
  - Utwórz rolę: nazwa, kolor, hoist, mentionable
  - Usuń: potwierdzenie z nazwą roli
  - Nadaj/odbierz: lista wszystkich członków, wyszukiwarka, badge "Ma rolę", przyciski Nadaj/Odbierz
  - Członkowie: lista kto ma rolę, wyszukiwarka

  **Uprawnienia (37 total):**
  - Ogólne (8): Administrator, ManageGuild, ManageRoles, ManageChannels, ManageWebhooks, ManageEmojis, ViewAuditLog, ViewGuildInsights
  - Członkowie (8): KickMembers, BanMembers, MuteMembers, DeafenMembers, MoveMembers, ManageNicknames, ChangeNickname, ModerateMembers
  - Wiadomości (14): SendMessages, SendMessagesInThreads, CreatePublicThreads, CreatePrivateThreads, ManageMessages, ManageThreads, EmbedLinks, AttachFiles, ReadMessageHistory, MentionEveryone, UseExternalEmojis, UseExternalStickers, AddReactions, UseApplicationCommands
  - Kanały (7): ViewChannel, Connect, Speak, Stream, UseVAD, PrioritySpeaker, RequestToSpeak

---

## Commit 12: `0ee9857` — Fix: scrollbar motyw, toggle switch OFF widoczny

### Co zmieniono
- `panel/app/globals.css`:
  - Scrollbar globalny (webkit + firefox): `scrollbar-width: thin`, thumb `var(--border-color)`, hover `var(--text-muted)`, track przeźroczysty, rozmiar 6px
  - Toggle switch OFF: tło zmienione z `var(--border-color)` (niewidoczne) na `rgba(var(--surface-rgb), 0.8)` z obramówką `var(--text-muted)` — wyraźnie odróżniony od tła
  - Kółko toggle OFF: `var(--text-muted)` zamiast białego — widoczne w obu trybach
  - Kółko toggle ON: białe na tle accent-color
  - `transform: translateY(-50%)` — kółko perfekcyjnie wyśrodkowane pionowo
- `panel/app/dashboard/layout.jsx`:
  - `.sidebar-nav`: dodany scrollbar webkit 4px (węższy niż globalny) dopasowany do wąskiej kolumny

---

## Commit 13: `a139c78` — DESIGN.md: system ról hierarchicznych, STATE.md: priorytety

### Co zmieniono
- `.arena/DESIGN.md`: nowa sekcja "SYSTEM RÓL HIERARCHICZNYCH":
  - 3 typy ról: standardowa / nadrzędna (parent, pusta) / podrzędna (child, należy do grupy)
  - Automat bota: listener `guildMemberUpdate` → nadaje/odbiera rolę nadrzędną automatycznie
  - Schemat bazy `role_groups`: guildId, parentRoleId, childRoles[], mode (single/multiple)
  - UI plan: rozwijane zakładki dla grup, dwa przyciski tworzenia
  - Kolejność implementacji: baza → endpoint → panel
- `.arena/STATE.md`: dodany priorytet #4 (role hierarchiczne) i sekcja statusu

---

## Commit 14: `7791d6b` — Fix: wylogowanie — httpOnly cookie nie było usuwane

### Co zmieniono
- `panel/app/api/auth/logout/route.js` — NOWY PLIK:
  - `POST /api/auth/logout` — usuwa httpOnly cookie `session` po stronie serwera
  - Cookie jest httpOnly — nie da się go usunąć przez JS w przeglądarce
- `panel/app/dashboard/layout.jsx`:
  - `confirmLogout` zmieniony z synchronicznego na `async`
  - Najpierw `POST /api/auth/logout` → usuwa cookie
  - Potem `localStorage.removeItem('session')` → usuwa localStorage
  - `window.location.href = '/'` zamiast `router.push('/')` — pełny reload, brak pozostałości stanu
  - Przyczyna błędu: `page.jsx` przy ładowaniu sprawdzał `/api/auth/me` → cookie nadal istniało → sesja była przywracana → redirect na /servers

---

## Commit 15: `1db073a` — Refactor: strona serwerów — baner, ciemne okno, wylogowanie w stopce

### Co zmieniono
- `panel/app/servers/page.jsx` — przepisany w całości:
  - Baner `baner-dashboard.png` jako górna część okna
  - Logo + nazwa bota w lewym dolnym rogu banera
  - Okno `max-width: 680px` wyśrodkowane na pustym tle `var(--bg-color)`
  - Czarne tło okna (`#000`)
  - Header: tytuł "Wybierz serwer" po lewej, avatar + nazwa zalogowanego usera po prawej
  - Lista serwerów z ikoną, nazwą, ID i dwoma przyciskami (Dodaj bota / Zarządzaj)
  - Lista z `max-height: 420px` i scrollem wewnętrznym
  - Stopka: "Zalogowany przez Discord OAuth2" po lewej, przycisk Wyloguj po prawej
  - Modal wylogowania z potwierdzeniem (używa /api/auth/logout)

---

## Commit 16: `ad0bfe8` — Fix: baner i logo wewnątrz okna, tło puste

### Co zmieniono
- `panel/app/servers/page.jsx`:
  - Baner przeniesiony z zewnątrz do środka okna
  - Tło strony (`.sp-root`): `display: flex; align-items: center; justify-content: center` — okno wyśrodkowane na środku ekranu
  - Usunięty `flex-direction: column` z root — okno teraz naprawdę wyśrodkowane pionowo
  - `.sp-window-wrap` uproszczony — tylko `width: 100%; max-width: 680px`
  - Gradient banera: `rgba(0,0,0,0) → rgba(0,0,0,0.65) → #000` — płynne wtopienie w czarne tło okna

---

## Commit 17: `c97d602` — Feat: jeden przycisk Zarządzaj/Zaproś zależnie od statusu bota

### Co zmieniono
- `panel/app/servers/page.jsx`:
  - Nowy stan `botStatus: { [guildId]: boolean | undefined }` — undefined = sprawdzanie
  - Po załadowaniu sesji: `Promise.all` sprawdza `GET /api/guilds/:id/stats` dla każdego serwera równolegle
  - Bot jest (200 OK) → przycisk "Zarządzaj" (btn-success, FiSettings)
  - Bota nie ma (404) → przycisk "Zaproś bota" (btn-success, FiPlusCircle) z linkiem Discord OAuth z `guild_id=`
  - Podczas sprawdzania → tekst "sprawdzanie..."
  - Usunięte dwa osobne przyciski "Dodaj bota" i "Zarządzaj"

---

## Commit 18: `abaef74` — Fix: async w useEffect, oba przyciski zielone

### Co zmieniono
- `panel/app/servers/page.jsx`:
  - Błąd: `await` w synchronicznym callbacku `useEffect(() => { ... })`
  - Fix: wyodrębniona wewnętrzna funkcja `const init = async () => { ... }; init();`
  - Przycisk "Zaproś bota": `btn-standard` → `btn-success` (zielony jak "Zarządzaj")

---

## Commit 19: `7d32387` — Refactor: strona logowania — okno z baner.png, logo wystające

### Co zmieniono
- `panel/app/page.jsx` — przepisany w całości:
  - Dodany stan `checking` — podczas sprawdzania sesji pokazuje puste czarne tło (brak flasha)
  - Układ: okno `max-width: 420px` wyśrodkowane na czarnym tle `#0a0a0f`
  - Baner: `baner.png` (1744×608, wyższy format) zamiast `baner-dashboard.png` — lepiej wygląda w wąskim oknie
  - Logo: `margin-bottom: -40px; z-index: 1` — logo "wyskakuje" z banera, przykrywa granicę baner/treść
  - Treść: tytuł, opis, przycisk Discord (kolor #5865f2 — oficjalny Discord, nie btn-base bo brak useTheme)
  - Stopka: informacja że logowanie przez discord.com, aplikacja nie jest wymagana
  - Stare: SVG Discord inline jako ikona + gradient overlay na banerze

---

## Commit 20: `edf3d3a` — AGENT.md: złota zasada log przed push

### Co zmieniono
- `.arena/AGENT.md`:
  - Proces pracy przebudowany: log jest teraz krokiem 5 przed push (krok 6)
  - Dodana "Złota zasada: NIE PUSHUJ bez logu"
  - Sekwencja: commit → log commit → push (nie: commit → push → log)
  - Wyjaśnienie dlaczego: po pushu pojawia się pokusa odkładania logu

---

## Commit 21: `(pending)` — Fix: strona logowania — logo nad tytułem, motyw CSS variables

### Co zmieniono
- `panel/app/page.jsx`:
  - Dodano `import { useTheme }` — strona teraz używa systemu motywu
  - `accentColor` z `useTheme()` — tytuł i przycisk reagują na kolor akcentu
  - Logo przeniesione z banera do `.lp-body` (nad tytułem) — nie jest już ucięte
  - Logo: `border: 1px solid var(--border-color)`, tło `rgba(var(--surface-rgb))`, padding 6px
  - `.lp-window`: zmienione z hardkodowanego `background: #000` na `rgba(var(--surface-rgb), var(--surface-opacity))` + `backdrop-filter: blur(12px)`
  - `.lp-body`: `rgba(var(--surface-rgb), 0.4)` — subtelne wyróżnienie sekcji treści
  - `.lp-banner-overlay`: gradient do `var(--bg-color)` zamiast `#000` — działa z light mode
  - Przycisk: `background: accentColor` zamiast hardkodowanego `#5865f2`
  - Wszystkie kolory tekstu: `var(--text-color)` i `var(--text-muted)` zamiast hardkodowanych
  - `checking` state: tło `var(--bg-color)` zamiast `#0a0a0f`

---

## Commit 22: `52e6b85` — Fix: proxy PATCH/DELETE/PUT, strony placeholder

### Co zmieniono
- `panel/app/api/proxy/[...path]/route.js`:
  - Przepisany na wspólną funkcję `proxyRequest(request, params, method)`
  - Dodane metody: `PATCH`, `DELETE`, `PUT` — wcześniej ich brak powodował że edycja i usuwanie ról zawsze kończyły się błędem
  - `DELETE` i `GET` nie wysyłają body (poprawne zachowanie HTTP)
  - Content-Type: teraz przekazywany z odpowiedzi bota zamiast zawsze ustawiać `application/json` — poprawia obsługę błędów HTML (502/504)
  - Concat zamiast template literal w URL bota

- Nowe pliki — strony placeholder (wcześniej 404):
  - `panel/app/dashboard/tickets/page.jsx` — FiMessageSquare
  - `panel/app/dashboard/automod/page.jsx` — FiZap
  - `panel/app/dashboard/welcome/page.jsx` — FiSmile
  - `panel/app/dashboard/logs/page.jsx` — FiClipboard
  - Każda: ikona w accentColor, tytuł, komunikat "w trakcie budowy"
  - Każda używa `useTheme()` — reaguje na motyw

---

## Commit 23: `5a0bcf7` — Fix: hardkodowane kolory/marginesy, template literals, error state config

### Co zmieniono

- `panel/app/dashboard/config/page.jsx`:
  - Dodany `error` state + funkcja `loadConfig()` — error state zamiast cichego błędu
  - Wyodrębniona funkcja `loadConfig` możliwa do wywołania ponownie (przycisk "Spróbuj ponownie")
  - Fetch naprawiony: usunięte template literals → konkatenacja, dodany `safeFetch` pattern (text → JSON.parse)
  - Wszystkie hardkodowane kolory (`#14141c`, `#25252d`, `#1e1e26`, `#6b6b76`, `#fff`) → CSS variables
  - Margines `100px 200px` → `max-width: 800px; margin: 0 auto; padding: 1.5rem`
  - Toggle naprawiony — opis pod labelem zamiast obok

- `panel/app/dashboard/page.jsx`:
  - Template literals w 6 fetch() → konkatenacja stringów
  - `.catch(() => ({}))` na każdym fetchu — brak danych nie crashuje całej strony
  - Hardkodowane kolory (`#14141c`, `#25252d`, `#6b6b76`, `#0a0a0f`, `#8b8ba0`) → CSS variables
  - Margines `100px 200px` → `padding: 1.5rem; max-width: 1100px`
  - Stat cards: `rgba(var(--surface-rgb))` + `backdrop-filter: blur` — reaguje na motyw
  - Responsive: 3 kolumny → 2 kolumny (≤900px) → 1 kolumna (≤600px)
  - Refaktoryzacja stat-cards na tablicę obiektów — mniej powtórzeń

- `panel/app/dashboard/bot-settings/page.jsx`:
  - Template literal w `manageCommands`: `` `/api/proxy${endpoint}` `` → `'/api/proxy' + endpoint`
  - Margines `100px 200px` → `margin: 0; max-width: 1100px`

---

## Commit 24: `7b82d36` — Fix: CSS variables moderation/settings, template literal layout.jsx

### Co zmieniono
- `panel/app/dashboard/moderation/settings/page.jsx`:
  - Wszystkie hardkodowane kolory w `<style jsx>` → CSS variables:
    - `#14141c` → `rgba(var(--surface-rgb), var(--surface-opacity))`
    - `#25252d`, `#1e1e26` → `var(--border-color)` / `var(--bg-color)`
    - `#1a1a24`, `#202028` → `rgba(var(--surface-rgb), 0.5/0.8)` (section-header hover)
    - `#a5b4fc` → `var(--accent-color)` (nagłówki sekcji)
    - `#6b6b76` → `var(--text-muted)`
    - `#fff` (teksty) → `var(--text-color)`
    - `#5865f2` (range, radio, range-value) → `var(--accent-color)`
  - Dodane `backdrop-filter: blur(12px)` na `.config-section`
  - `.toggle`, `.toggle-label`, `.command-permissions`: border + surface bg
  - `.radio-label`, `.toggle-header span`: color z var

- `panel/app/dashboard/layout.jsx`:
  - Ostatni template literal: `` `/api/proxy/api/guilds/${selectedGuildId}/stats` `` → konkatenacja

---

## Commit 25: `6ea259b` — Fix: globalny wrapper stron z ramką accentColor

### Co zmieniono
- `panel/app/dashboard/layout.jsx`:
  - Dodany `.page-content-wrapper` wokół `{children}` — border 1px accentColor, margin 1.5rem, border-radius
  - Zastępuje `margin: 100px 200px` + `border: 1px solid accentColor` które były hardkodowane w każdej stronie osobno
  - Każda strona dashboardu automatycznie dostaje ramkę i odstęp od krawędzi — zero zmian w samych stronach
  - `.main-content`: dodany `overflow-y: auto` — scroll na poziomie main zamiast każdej strony

---

## Commit 26: `2cee843` — Fix: wrapper wyśrodkowany, padding na main-content

### Co zmieniono
- `panel/app/dashboard/layout.jsx`:
  - `.main-content`: `overflow: hidden` → `overflow-y: auto`, dodany `padding: 2rem`, `background: var(--bg-color)`
  - `.page-content-wrapper`: `margin: 1.5rem` → `max-width: 1100px; margin: 0 auto` — okienko wyśrodkowane z przestrzenią po bokach (padding z main-content), ramka obejmuje tylko treść
